<odoo>
  <data>
    <record id="stock_picking_inherit_form" model="ir.ui.view">
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="model">stock.picking</field>
        <field name="arch" type="xml">
            <field name="location_id" position="replace">
                <field name="location_id" options="{'no_create': True}" groups="stock.group_stock_multi_locations" attrs="{'invisible': [('picking_type_code', '=', 'incoming')]}" domain="[]"/>
            </field>
            <field name="location_dest_id" position="replace">
                <field name="location_dest_id" options="{'no_create': True}" groups="stock.group_stock_multi_locations" attrs="{'invisible': [('picking_type_code', '=', 'outgoing')]}" domain="[]"/>
            </field>
        </field>
    </record>
  </data>
</odoo>